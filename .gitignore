__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Local config & secrets
config.local.yaml
.env
.env.*

# Runtime artifacts
logs/*.log
logs/*.log.*
database/*.db
database/*.db.backup
*.bak

# Phase 0 historical data store — keep the dir, exclude the parquet payloads
data/**/*.parquet
data/*.parquet
!data/.gitkeep
!data/regimes/.gitkeep

# Track F experiment storage — keep the dirs, exclude the payloads
experiments/log/*.json
experiments/*.parquet
!experiments/.gitkeep
!experiments/log/.gitkeep

# Editor / OS
.vscode/
.idea/
.DS_Store

# Local Claude state (agent worktrees, local settings)
.claude/
